<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple BBS</title>
    <link rel="stylesheet" href="/static/css/bootstrap-reboot.min.css">
    <link rel="stylesheet" href="/static/css/stylesheet.css">
    <link rel="shortcut icon" href="/static/favicon.ico" type="image/x-icon">
    <script src="https://kit.fontawesome.com/0e24b8315a.js" crossorigin="anonymous"></script>
</head>

<body>
    <header>
        <nav class="container navbar">
            <!-- Logo(Link to Home) -->
            <a class="navbar-brand" href="/">Simple BBS</a>
        </nav>
    </header>

    <main>
        <div class="container">
            <form id="search-box" action="/threads" method="get">
                <input type="text" name="keyword" id="keyword"
                    placeholder="スレッドタイトル検索"
                    value="{{keyword}}">
                <button><i class="fas fa-search"></i></button>
            </form>

            <div class="threads">
                {{#each threads}}
                <div class="thread-row">
                    <div class="thread-title"><a href="/thread/{{id}}">{{title}}</a></div>
                    <div class="thread-info">
                        <div class="responses"><i class="fas fa-comments"></i>{{responses}} responses</div>
                        <div class="last-updated"><i class="far fa-clock"></i>{{updated_at}}</div>
                    </div>
                </div>
                {{else}}
                <div class="thread-row">
                    <div class="thread-title">該当スレッドなし</div>
                </div>
                {{/each}}
            </div> <!-- /threads -->

            {{#if pagination}}
                {{!-- {{log keyword}} --}}
                {{> partial_pagination pagination=pagination keyword=keyword}}
            {{/if}}

            <div class="user-form">
                <h3 class="user-form-header">新しいスレッドをつくる</h3>
                <form action="" method="post">
                    <input type="text" name="new-thread-title" id="new-thread-title" placeholder="タイトル（必須）">
                    <input type="text" name="username" id="username" placeholder="名前（省略可）">
                    <textarea name="comment" id="comment" placeholder="コメント（必須）"></textarea>
                    <button class="btn-new-thread">
                        <i class="fas fa-check"></i>スレッド作成
                    </button>
                </form>
            </div>                

        </div> <!-- /container -->
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2020 tazeen</p>
        </div>
    </footer>

    <script src="/static/js/script.js"></script>
</body>

</html>